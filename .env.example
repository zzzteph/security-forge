# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# --- Optional: private target repo auth -------------------------------------
# If the target is private, either embed a token in config.yaml's target.repo
# URL, or set a GitHub token here and reference it in the URL as needed.
# GITHUB_TOKEN=
